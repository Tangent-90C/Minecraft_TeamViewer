# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx1G

# Minecraft/Fabric version profiles live in gradle/minecraft-versions.properties.
# The default target is 1.21.8; select another target with -Pmc_target=<version>.

# Mod Properties
	mod_version = v0.4.14-proto0.6.2
	maven_group = fun.prof_chen
	archives_base_name = TeamViewRelay
	network_protocol_version = 0.6.2
	network_min_compatible_protocol_version = 0.6.1
	protobuf_version = 4.35.1
