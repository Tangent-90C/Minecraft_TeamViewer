package fun.prof_chen.teamviewer.main_code.client.sdk;

import fun.prof_chen.teamviewer.main_code.config.ui.ConfigUiController;

/** Opens the version-native widget host for the common seven-page configuration session. */
public interface ConfigScreenHost {
    void open(ConfigUiController controller);
}
